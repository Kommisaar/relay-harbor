{
  "$comment": [
    "CON-008/ADR-007 依赖白名单：npm install 的包必须先进入本名单。",
    "禁 Fluent v8（@fluentui/react）、禁 Tailwind、禁编辑器（CodeMirror）、禁 dnd、@xyflow 待 M2。",
    "Rust 依赖由分层边界检查（scripts/check-rust-boundaries.mjs）与实现任务约束。",
    "2026-08-28 UI 实现：i18next + react-i18next 准入（界面双语 FR-016/UI-004，用户确认按需引入）。",
    "2026-08-28 图表选型（用户确认）：概览图表采用 @ant-design/charts（评价 Fluent charts 不成熟，callout 字号缺陷）。",
    "2026-08-28 @ant-design/graphs 预准入：M2 关系图可视化采用（替代此前预留的 @xyflow）；M2 启动时再安装，当前不装避免未用依赖。",
    "@fluentui/react-charts 已移出：2026-08-28 曾准入后被上述选型决定替代。"
  ],
  "dependencies": [
    "@ant-design/charts",
    "@fluentui/react-components",
    "@fluentui/react-icons",
    "@tanstack/react-query",
    "@tauri-apps/api",
    "react",
    "react-i18next",
    "i18next",
    "react-dom",
    "react-markdown",
    "react-router-dom",
    "zustand"
  ],
  "devDependencies": [
    "@tauri-apps/cli",
    "@types/react",
    "@types/react-dom",
    "@vitejs/plugin-react",
    "dependency-cruiser",
    "typescript",
    "vite"
  ]
}
