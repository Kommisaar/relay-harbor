// 条目浏览/详情/修订/关联/影响（UC-011/012/015）。
// queries 落地时 key 以 ["projects", projectId, ...] 开头（ADR-006 失效前提）。
import { PlaceholderPage } from "../../../components/PlaceholderPage";

export function DesignPage() {
  return <PlaceholderPage title="设计条目" />;
}
