# 一致性检查

## 结论

- 检查基线：00～05 全部已确认（2026-08-27）；OQ-001～006 已解决
- 结果：**通过**（四项建议级发现均已当场修订闭环，复核确认）
- 阻塞项：0
- 风险项：0 开放（已接受风险见 risks.md）

## 发现

### FINDING-001 用例开放问题未回填 05 定案

- 级别：建议
- 证据：UC-009 仍写"拉起时限留 05"、UC-016 仍写"导出结构留 05"，
  而 05 已定案（15 秒时限、目录结构、修订粒度）
- 影响：文档间追踪断链，实现者可能重复决策
- 最早负责阶段：02 用例
- 处理方式：回填两份规约的开放问题段，指向 05 定案位置
- 状态：已解决（同轮修订）

### FINDING-002 概览概念 Plan 无下游承载

- 级别：建议
- 证据：00 核心概念含 Plan，01 需求、02 用例、03 模型均无承载或占位
- 影响：正向追踪缺口；可能被误读为 M1 范围
- 最早负责阶段：00 概览（概念引入）
- 处理方式：在 03 domain-model.md 增加"未建模的概览概念（Plan）"小节，
  显式声明无承载、进入范围时的修订路径
- 状态：已解决（同轮修订）

### FINDING-003 INV-* 为约定外扩展编号

- 级别：建议
- 证据：dev-toolkit 文档约定前缀集合不含 INV，但 03/04/05 多处引用
- 影响：跨文档编号纪律偏差
- 最早负责阶段：03 领域模型
- 处理方式：03 README 显式声明 INV（与 SEQ）为扩展编号及定义处
- 状态：已解决（同轮修订）

### FINDING-004 同项目约束归属未写明

- 级别：建议
- 证据：INV-003 要求关系两端同项目，data-model 引用完整性节未说明
  该约束由哪层保证（外键无法表达）
- 影响：实现者可能漏掉该校验
- 最早负责阶段：05 详细设计
- 处理方式：data-model 补充"领域层事务内校验"说明
- 状态：已解决（同轮修订）

## 已验证项目

- 阶段状态：00～05 全部已确认，无"需修订"未恢复项；
- 开放问题：OQ 全部关闭；散落于用例的开放问题全部回填关闭；
- 正向追踪：16 FR 全部有 UC 承载且覆盖表一致；9 NFR 全部有架构响应与
  验证方式；9 CON 无违反；
- 反向追踪：8 CMP、6 INT、8 ADR、12 工具均有需求/质量属性来源，
  未发现镀金设计（snapshot/baseline 有明确承载或占位理由）；
- 语义一致性：BR-002 白名单 ↔ 03 双状态机 ↔ data-model 状态值一致；
  BR-009 在 UC-004、状态图、edit_item 返回值三处一致；影响定位入边
  闭包语义在 DOM-003/get_context/get_impact 三处一致；SEQ-001/002
  参与者方向与 INT 提供方/使用方一致；错误码与模块错误处理一致；
  令牌/权限模型（http 唯一暴露面、前端零令牌、ipc 受信）各层一致；
- 文档完整性：根索引与各阶段索引链接有效（本轮逐一核对）；编号无
  重复；Mermaid 图（flowchart/classDiagram/stateDiagram/erDiagram/
  sequenceDiagram）均为目标渲染器支持语法。

## 未覆盖范围

- 代码级验证（编译、测试执行）不在设计验证范围，属实现阶段；
- M2 条目（基线流程、导入、快照、关系图）未编号未追踪，按 01 README
  声明属预期。
