# 开放问题

## OQ-001 数据事实来源与条目粒度

- 状态：已解决
- 来源阶段：00-overview
- 影响：需求编辑方式、领域模型、存储设计、MCP 契约、导入导出和 Git 审查流程
- 是否阻塞：是
- 最晚决策点：确认 00 项目概览前
- 当前候选：
  - 数据库作为唯一运行时事实来源，条目使用结构化元数据加 Markdown 正文，Markdown/JSON 仅作为确定性快照（当前建议）；
  - Markdown 作为事实来源，数据库只做索引和缓存；
  - 数据库与 Markdown 双向可编辑，但需要复杂的冲突解决机制，不建议首期采用。
- 结论：采用候选一（2026-08-27 用户确认）：数据库为唯一运行时事实来源，条目为结构化元数据加 Markdown 正文；导出 Markdown/JSON 仅为确定性快照。

## OQ-002 首期产品边界

- 状态：已解决
- 来源阶段：00-overview
- 影响：功能需求、首期交付规模、任务领域模型和安全边界
- 是否阻塞：是
- 最晚决策点：确认 00 项目概览前
- 当前候选：
  - 首期只实现设计与任务管理平面，执行 Agent 只读取任务和回写状态（当前建议）；
  - 首期同时实现 Agent 派发、Worktree、Git 集成和执行监控；
  - 分成管理 MVP 与执行编排两个独立里程碑。
- 结论：采用候选一（2026-08-27 用户确认）：首期仅实现设计与任务管理平面，执行 Agent 只读取任务和回写状态；派发、Worktree、执行监控全部为未来候选。2026-08-27 修订：M1 的管理写入全部经 MCP 由 Agent 完成，UI 定位只读与 Markdown 导出；管理平面的范围结论不变。

## OQ-003 远程数据库进入哪个版本

- 状态：已解决
- 来源阶段：00-overview
- 影响：存储抽象、身份认证、权限、部署和测试矩阵
- 是否阻塞：否
- 最晚决策点：进入 04 架构前
- 当前候选：
  - MVP 只实现 SQLite，同时保留存储适配边界；后续仅增加 PostgreSQL（当前建议）；
  - MVP 同时实现 SQLite 和 PostgreSQL；
  - 仅支持 PostgreSQL，由用户自行部署。
- 结论：采用候选一（随技术方案评审确认，见仓库根《project structure.md》）：MVP 仅实现 SQLite；Storage 端口保留。2026-08-27 修订：远程演进形态定为独立 Remote Server（同一领域核心的第二宿主），PostgreSQL 仅作为 Server 端存储，桌面应用不直连远程数据库。

## OQ-004 应用与 Plugin 的安装和启动关系

- 状态：已解决
- 来源阶段：00-overview
- 影响：安装体验、MCP 配置、版本握手、更新和故障恢复
- 是否阻塞：否
- 最晚决策点：进入 04 架构前
- 当前候选：
  - 桌面应用独立安装，Plugin 携带 MCP 声明和轻量连接器；连接器发现或启动应用（当前建议）；
  - 桌面应用作为 Plugin Sidecar 一起分发；
  - 用户手动启动应用，连接器只负责连接。
- 结论：采用候选一（随技术方案评审确认）：桌面应用独立安装；Plugin 携带 MCP 声明与轻量连接器。连接器定位为 stdio↔HTTP 纯转发网关，负责发现或拉起应用与令牌引导，不含业务逻辑。2026-08-27 修订：连接器无论项目归属一律对接本地应用，远程项目经本地应用转发，连接器不持有用户凭据。

## OQ-005 首发操作系统

- 状态：已解决
- 来源阶段：00-overview
- 影响：托盘行为、安装包、自动更新、签名、路径和测试范围
- 是否阻塞：否
- 最晚决策点：进入 04 架构前
- 当前候选：
  - Windows 优先，代码保持跨平台边界；
  - Windows、macOS 和 Linux 同时首发；
  - Windows 与 macOS 首发，Linux 后续支持。
- 结论：采用候选一（2026-08-27 用户确认）：Windows 优先首发，代码保持跨平台边界。

## OQ-006 代码仓库与发布边界

- 状态：已解决
- 来源阶段：00-overview
- 影响：目录结构、版本策略、CI、Release 和 Plugin 兼容矩阵
- 是否阻塞：否
- 最晚决策点：开始实现前
- 当前候选：
  - RelayHarbor 使用独立仓库，`agent-toolkits` 只维护 Plugin 和兼容契约（当前建议）；
  - RelayHarbor 与 `dev-toolkit` 保持在同一 Monorepo；
  - 先在当前仓库验证原型，稳定后拆分。
- 结论：采用候选一：RelayHarbor 独立仓库（本仓库已建立），`agent-toolkits` 只维护 Plugin 与兼容契约。

## OQ-007 条目模型外设计工件的纳入方式

- 状态：部分解决（浏览页子项 2026-09-02 回退，重议中）
- 来源阶段：05-detailed-design（2026-09-01 界面设计扩展）
- 影响：条目类型集合、第二层子导航、IPC 命令面、导出产物结构
- 是否阻塞：否
- 最晚决策点：M1 实现前（类型集合冻结）
- 当前候选：
  - 全部升格为条目类型（modules/deployment/data-model 等也入类型）；
  - 全部走设计文档浏览页，条目模型不动；
  - 混合：编号化规格升格条目类型，长文档走浏览页。
- 结论：候选三部分执行，长文档子项回退：
  - **UI-xxx 升格为第 14 条目类型**（2026-09-01 用户指令，已实施：
    编号体系已被代码注释广泛引用，原子粒度与 FR/ADR 同构；插序
    SEQ 之后、ADR 之前）；
  - ~~长文档走设计文档浏览页~~（2026-09-01 决策，**2026-09-02 用户
    指令暂缓回退**：页面/入口/规格/两命令全部移除，条目模型外工件
    维持现状不纳入；回退留痕见 ui/README.md 修订记录）；
  - 追踪矩阵页暂缓（traceability 职责由条目关系 traces/satisfies
    承载，可视化页待需求出现再议）。

## OQ-008 全局滚动条自定义样式

- 状态：未决（2026-09-02 实现后同日滚回，重议中）
- 来源阶段：05-detailed-design（2026-09-02 界面微调）
- 影响：styles.css 全局样式、主题解析 DOM 钩子（AppProviders）、
  各滚动区观感（侧栏/内容区/各页）
- 是否阻塞：否（系统默认滚动条可用）
- 最晚决策点：无硬性时点（纯观感项，可与任意 UI 轮次搭车实施）
- 当前候选：
  - 维持系统默认滚动条（现状）；
  - 细线型 4px：常显细圆角拇指 + 透明轨道，hover 加深（VS Code 风）；
  - 悬浮型 8px：拇指平时透明、悬停所在滚动区浮现、悬停拇指加深
    （Windows 11 应用内观感；2026-09-02 曾实现后滚回）。
- 结论：待议。已验证的技术结论备查：`::-webkit-scrollbar` 系伪元素
  在开发浏览器与 Tauri WebView2（同为 Chromium）表现一致，上述任一
  方案均可实现；暗色跟随需把 UI-003 解析主题写入 DOM（如
  `documentElement[data-theme]`，Fluent v9 token 为 JS 值不进 CSS）；
  不建议 `scrollbar-width`/`scrollbar-color` 标准属性（无圆角/透明
  轨道控制力）与 `prefers-color-scheme`（手动主题档失配）。
