# 全局界面模式

> 状态：待确认
> 关联：UI-030～032、FR-011/012、NFR-008、ADR-006/007

## 状态徽章语义色（UI-030）

条目与任务状态用 Badge 呈现，色彩映射固定（Fluent Badge appearance +
tokens，具体色值由主题令牌决定，不硬编码）：

| 状态 | 语义 | Badge 档位 |
| --- | --- | --- |
| 草稿 draft | 未定稿 | neutral（灰） |
| 评审中 in_review | 待人审 | warning（琥珀） |
| 已确认 confirmed | 定稿 | success（绿） |
| 已取消 cancelled | 终态-未确认即放弃 | danger（红） |
| 已替代 superseded | 终态-被替代 | brand（紫/品牌色区分） |
| 已废弃 deprecated | 终态-确认后失效且无后继（2026-09-02 新增） | severe（橙红） |
| 待办 todo | 任务未开始 | neutral |
| 进行中 doing | 任务活跃 | brand（蓝） |
| 待验收 await_review | 任务完成待审 | warning |
| 完成 done | 任务终态 | success |
| 已取消（任务） | 任务终态 | danger |

同屏同时用文字（徽章内文案）表达状态，不依赖颜色单独区分（无障碍）。

## 空态（UI-031）

统一结构：图标 + 一句话 + 指引。各页面文案要点：

| 场景 | 提示要点 |
| --- | --- |
| 项目列表为空 | 「项目由 Agent 经 MCP 创建」——M1 UI 无创建入口（CON-009），指引写明写入通道 |
| 条目列表为空 | 项目尚无条目；同样提示由 Agent 写入 |
| 搜索无结果 | 「未找到匹配 XX 的条目」+ 建议检查编号/换关键词（FR-012 验收要求；页面 2026-08-28 暂缓移除，模式保留备恢复） |
| 看板某列为空 | 列头计数 0 + 轻提示，不占大块空态 |
| 详情修订仅 1 条 | 正常显示（v1 即当前），不显示空态 |
| 影响清单为空 | 「无受影响条目」——正向信息，非错误 |

## 加载态（UI-032）

- 列表与卡片类页面（项目列表、条目列表、看板、搜索结果、概览统计）：
  骨架屏占位，避免布局跳动；
- 局部动作（导出提交、版本切换）：按钮/区域 spinner；
- 事件失效触发的后台刷新：保持现有内容 + 静默 refetch（TanStack Query
  默认行为），不闪骨架屏。

## 错误态

统一错误组件（modules/frontend.md 既有约定）：错误说明 + 重试按钮；
查询失败页面级呈现。导出失败遵循 UC-016：原因 + 已完成部分说明，
不留误导性成功产物。

## 数据刷新

继承 ADR-006：写工具成功 → data-changed 事件 → 按项目前缀失效 →
静默 refetch；事件丢失由 refetchOnWindowFocus 兜底。UI 不做轮询。

## 只读边界的视觉表达

全局无任何写控件（按钮/表单提交）指向业务数据；唯一例外是设置页的
应用设置读写与导出面板的参数选择 + 目标路径写入（文件系统，非业务库）。
依赖白名单 lint 保障（无编辑器、无 dnd）。

## 盒模型（2026-09-01 全局迁移）

styles.css 声明全局 `box-sizing: border-box`（`*`/`::before`/`::after`）：
显式 width/height 即最终占位，含 padding 与 border。迁移动机：content-box
下 `width: 100%` + padding 会溢出容器（实例：活动栏展开态条目右侧高亮被
`overflow-x: hidden` 裁切）。

- Fluent v9 组件自带 border-box 声明，不受迁移影响；裸元素（原生
  a/button/div）不再逐个声明 box-sizing；
- 迁移等值调整：第二层侧栏行 minHeight 44/40（原 36/32 + 垂直 padding 8）、
  各页容器 maxWidth +48（原口径只算内容宽）；侧栏总宽回归本文档 232px
  口径（迁移前 content-box 膨胀至实际渲染 249）；
- 任务看板 `height: 100%` 不再叠加垂直 padding 撑出滚动（迁移前内容区
  恒有 32px 幽灵滚动）。

## 工具条（2026-09-01）

页首「标题 + 过滤控件」工具条为不换行 flex 行：标题 `flexShrink: 0` 恒单行
（CJK min-content 仅一字宽，标题不可作为收缩方）；空间不足由过滤控件收缩
让宽，下限 `minWidth: 120px`（须覆盖 Fluent Dropdown 默认 min-width 250px，
否则全部弹性落在标题上导致竖排挤压）。

## Markdown 正文（2026-09-02，UI-035 随概览文档改版升为共享模式）

条目正文与项目概览正文统一由共享组件 `MarkdownBody`（`src/components/`，
react-markdown + remark-gfm 封装；GFM 表格为维护型文档常态，react-markdown
v10 裸用不解析，remark-gfm 白名单 2026-09-02 准入）渲染。排版规则不走
Griffel：Markdown 生成的子元素集合开放，无法逐一建样式，故采用
`src/styles.css` 作用域类 `.md-body`（全局样式的刻意例外，组件注释留痕），
覆盖标题层级/列表/引用/代码块/表格/分隔线；组件本身只承担 article 语义
与行高口径，不干预内容。消费方：条目详情（UI-017）、项目概览（UI-035）。

## 修订对比（单栏 AST Diff，2026-09-03 用户指令；方案 markdown-unified-diff）

正文区默认显示修订快照全文；是否与紧邻上一版做单栏合并 diff 由
**「与上一版对比」手动开关**控制（2026-09-03 同日三改，废止原「查看
历史版自动 diff」口径与 MessageBar 提示条）：相同块只渲染一次，删除/
新增块按原序交错。开关位于修订历史分区标题行右缘、收起按钮左侧
（subtle 图标 ToggleButton，tooltip「与上一版对比」，选中态图标/底色
提示开启）；无上一版（最旧版/仅 1 条修订）时禁用。开关对当前版同样
适用——开 + 当前版 = 最新修订与紧邻上一版对比（原「自动 diff」下
不可达的场景随之解锁）。`diffOn` 为页面本地 state：默认关、跨修订
切换保持、离开页面重置；对比基准取修订序列前一项，不假设编号连续。
查看状态信号由既有元素自明：时间线选中圆点、收起态胶囊 vN 徽标、
diff 标记本身；「回到当前」= 点时间线当前版（既有行为，随提示条
废除不再单设按钮）。消费方：条目详情（UI-017）、项目概览（UI-035）。
口径与实现约束：

- **比较渲染语义**（非源码/空行切分）：`*文本*` 与 `_文本_`、列表符号、
  空白排版差异不产生差异；块移动首版按删除+新增呈现；不做词级/字符级
  diff、块移动识别与后端预计算。
- **纯前端 MDAST 路径**：unified + remark-parse + remark-gfm 解析
  before/after（与 MarkdownBody 共享同一套 GFM 选项），引用式链接/
  图片按 definition 自包含化（防合并树 identifier 冲突），语义规范化
  （剥 position/data）后块序列对齐——Myers/LCS 锚定相同块 + 相邻未
  匹配区间有上限的最小成本配对，超限保守降级整块增删；容器递归范围：
  同型列表 listItem、表格 tableRow（列结构/对齐/表头变化整表增删）、
  同语言代码块行级、blockquote 顶层块；HTML/图片/分隔线按原子节点。
- **紧凑列表项整项标记（2026-09-03 走查实测修订）**：紧凑项
  （spread=false）的内层段落会被 remark-rehype 解包，段落上的
  data-diff 属性进不了 DOM（新旧文本裸拼在同一 li、标记静默丢失），
  故紧凑项变化整项标删除+新增；spread 项内层段落保留、继续递归做
  块级标记。有序列表的删除/新增项写显式 `value`（li 合法属性），
  浏览器从 value 续排，后续项不因增删项扰动编号。
- **单实例渲染**：共享 `MarkdownDiffBody`（`src/components/markdown-diff/`，
  props 仅 `before`/`after` 两字符串）经自定义 remark 插件将合并 MDAST
  交给**单个** react-markdown 实例（不按片段起多渲染器），代码行由
  自定义 `code` renderer 按 DiffPlan 行操作输出 span；「何时显示
  diff」的页面交互不进入算法组件。
- **样式例外留痕**：同 `.md-body` 先例，diff 标记走 `src/styles.css`
  作用域类 `.md-diff-body`——增删背景/左边框用主题 CSS 变量（明暗
  自跟随），行首 `+/-` 伪元素标记（不只靠颜色）；表格行标记落在首格
  内部（`tr::before` 会挤出额外列）；删除块内链接去活化（渲染期去掉
  `href` 降为文本，不以 `pointer-events` 为唯一手段）。
- 无新增 IPC/Rust 逻辑（CON-009 只读边界不变）：比较输入为修订快照
  正文（`Revision` / `OverviewRevision` 的 `snapshot.bodyMd`），已随
  修订列表一次取齐；「目标修订 + 紧邻上一版」取 revisionNo 小于目标
  的最大一项，不假设修订号连续，也不用列表倒数第二项（详情与修订
  列表是两条独立查询）。

## 浮动胶囊面板（2026-09-03 用户指令升格为通用模式；修订时间线为其首个内容）

壳层自 `RevisionTimeline` 抽为共享组件 `CapsulePanel`（2026-09-03，随
条目详情页「关联」「影响定位」两区块迁入面板）：sticky 悬浮 wrapper、
胶囊按钮、同位变形面板、header、点外/Esc 关闭全部上收壳层，内容以
children 注入。多胶囊布局空白点（patterns 此前只规定单胶囊悬浮位）
同日拍板：**单胶囊多分区**——面板内以分割线分区，否决三胶囊并排
成簇与面板内 Tab。通用约定：

- **胶囊**：label + 图标 + 计数徽标（badge 为数量，0 显「无」，对齐
  空态文案口径）；「非默认态」以选中底色提示（如正在查看历史版）；
- **面板分区**：多分区纵向堆叠，分隔样式 2026-09-03 用户指令改样
  （参照样例 ZCode「Git 工具」面板）——**分区上缘一条全宽 hairline
  （无字 Divider）+ 线下左对齐弱化小节标题**（fg3 常规字重），弃居中
  夹字 Divider；首分区不另画线（上方即面板顶缘；原「复用 header 下缘
  线」随同日标题行废除失效）；**小节标题与各分区内容最左元素同一左缘**
  （同日用户指令「板块标题和内容要对齐」：左缘 24px = 分区内容边距
  16 + 修订圆点自带 8 边距——修订历史对齐圆点左缘、关联/影响定位对齐
  「满足」等首列；行 wrapper 原水平 8px margin 随之移除，轨道相对几何
  不变；收起按钮居首分区标题行右缘，见「展开面板」条）；**小节标题行
  统一 28px 高**（与行内收起按钮同高、文字垂直居中——同日用户指令：
  首分区行原被按钮撑高致「标题→内容」间隙 6px、其余分区 2px 不一致，
  统一后三区同为 6px）；关联/影响定位内容区另加顶部 8px 内边距，使
  分组标签首行文字与修订时间线首行文字的视觉位置一致（同日用户指令
  「其他两个模块的间距更近」——单选行行内几何使其文字天然更低）；内容区
  `maxHeight: min(60vh, 520px)` 超出内部滚动（三分区叠加可能超一屏
  ——同日新增约定）；空态「显示无而非隐藏」（UI-031）随迁；胶囊文字
  随内容域命名——条目详情三分区称「详情面板」（common.detailPanel），
  项目概览单分区沿用「修订历史」（面板自身不再有名称标题行，见
  「展开面板」条）；**小节标题区可点击展开/折叠对应分区**（2026-09-03
  用户指令）：标题后随 chevron（同日再指令移至标题后；折叠朝右、展开
  旋转 90° 朝下；**同日系列指令：默认隐藏、hover/键盘聚焦渐显、hover
  无底色反馈、标题不加手型（默认箭头）**——var 门控同胶囊 --icon-hover
  手法，opacity + transform 过渡
  同图标渐变曲线体系），标题区为独立 disclosure 按钮（`aria-expanded`
  + `aria-controls`），body 经
  grid-template-rows 1fr→0fr 高度过渡收展，内容 opacity 随行渐隐/渐现
  （2026-09-03 用户指令加渐入渐出——初始为纯高度；同日再反馈两向统一
  减速曲线，原收起方向加速曲线观感过急；时长 300ms = durationSlow
  令牌——本版令牌实值 durationNormal 200/durationSlow 300，非常见文档
  的 150/250）；与
  首分区右缘「收起面板」**语义分离——折叠分区 ≠ 收起整面板**，两控件
  并排独立不嵌套（HTML button 不可嵌套）；折叠状态为分区本地 state
  （默认展开；面板组件常挂载期间保留，不持久化——纯视图态，UI 只读
  边界不变）；标题文字左缘 24px 对齐不变（chevron 跟随文字之后，不占
  对齐线前导位置）；
- **消费方组合**：`<CapsulePanel>` 包单分区 `<CapsulePanelSection>`
  （修订历史 + 行内收起按钮，项目概览 UI-035，2026-09-03 用户指令
  统一条目详情面板样式）/ 包「修订历史 + 关联 + 影响定位」三分区
  （条目详情 UI-017）。

以下为修订时间线内容的既定结论（其中收起态胶囊/展开面板/关闭逻辑
各条同时是 CapsulePanel 壳层行为，2026-09-02 起逐条留痕）：

修订历史统一由共享组件 `RevisionTimeline` 呈现，2026-09-02 用户指令
由文末「Divider + 内联列表」区块改版为**右上角浮动胶囊**（原型参照
ZCode 右上角「更改」胶囊，内部排版不照搬、以交互逻辑为准）：

- **收起态胶囊**：圆角胶囊（高 40px + 14px 字——2026-09-02 用户指令
  由 small 升级「太小了」、同日再指令加高；**文字常规字重 400**
  （2026-09-03 用户指令，覆盖 Fluent Button 默认 SemiBold 600，计数
  徽标随按钮继承）；边框 + shadow8 浮起阴影；
  内容 = 图标 + 「修订历史」+ 计数圆角徽标；**hover 时历史图标经
  200ms 渐出、展开图标渐入（纯透明度过渡，2026-09-03 用户指令由
  交叉渐变旋出改为渐入渐出）**（双层图标叠放，`--icon-hover` CSS
  变量门控 + calc 派生透明度，规避 Griffel 后代选择器限制；
  未随「减弱动态」降级——动效轻量，同活动图 canvas 动画留痕策略）；
  同日尺寸统一留痕：Fluent medium 按钮图标槽仅 20×20，而双层叠放
  容器子层全 absolute（flex min-content=0）会被 flex-shrink 压到
  20 宽、图标以 ~0.83 倍渲染，与展开面板收起按钮的 svg（直排属性
  宽）形成两态大小跳变——容器加 flexShrink:0 统一两态渲染框
  （2026-09-03 用户指令「展开和收缩状态下两个图标大小不一致」，
  实测定案非错觉）；同日再指令「有点大了」定稿 16×16：胶囊双层与
  收起按钮同换 16 系列字形（16Regular，按 16px 专门设计优于缩放
  24 字形），双层容器随之 16×16）；
  2026-09-03 用户指令（随图指认宽窗口死区）改**视口右上角 fixed
  锚定**：top XXL（实值 24px）+ right 24px（令牌外值留痕）——原
  sticky 参照页面容器（文字列）右缘，宽窗口下容器 maxWidth 之外的
  留白使胶囊远离窗口缘；改后任何窗宽恒贴窗口右上、**永不随内容
  滚走**（原 2026-09-02 sticky「与页顶保持设定的最小距离」三次指令
  纠正语义、及受父容器边界约束文章末尾被顶走的语义，沿革留痕；同日
  查证订正：spacingVerticalXXL 令牌实值 24px，此前文档误标「XXL 32」
  ——历史实现实为 24px；与样例 ZCode「更改」胶囊视口锚定一致）；
  正在查看历史版时胶囊呈选中底色提示当前不在当前版。
- **展开面板**：点击胶囊开合（aria-expanded）；2026-09-03 用户指令
  改**原位变形**：面板与胶囊同一锚定位（顶边同线、右对齐，展开时
  胶囊直接 visibility hidden 隐藏——同日追加指令，不再仅靠面板
  覆盖），展开经 200ms 渐入 + 自右上角缩放由胶囊
  「变」为面板，收起反向渐出缩回胶囊（CSS transition 双向，组件
  常挂载 + visibility 延迟翻转保证离场动画可播；未随「减弱动态」
  降级——同前图标渐变留痕策略），elevation 阴影、不挤动正文；宽度
  360–480px 自适应（2026-09-03 用户指令收窄，原 440–560）；**面板无
  名称标题行**（2026-09-03 用户指令对照样例再改：样例展开面板无名称
  ——原 2026-09-02 header：标题「修订历史」居左 + 右侧缩小按钮
  ArrowMinimize、hairline 下缘与清单分隔、16px 常规字重，整行废除，
  沿革留痕）；收起按钮改**并入首分区小节标题行右侧**（同日指令：
  icon-only subtle ArrowMinimize、aria「收起面板」，点击与点外/Esc
  等效；同日三次指令：概览单分区面板亦统一此样式——首分区标题行
  「修订历史」+ 行内收起按钮；收起按钮 hover/按下保持中性灰仅浅色
  底（同日用户指令：不随 subtle 外观默认切品牌色前景——根上钉色外
  还须压图标槽：Fluent subtle 自带 `.hash:hover .fui-Button__icon`
  后代规则把图标切 NF2BrandHover、特异性高于根级钉色，实现经根上
  CSS 变量给图标 subtree 供中性灰墨，同胶囊 --icon-hover 手法））；面板行清单
  （2026-09-03 用户指令改 **RadioGroup/Radio 形态**）：原生单选圆点
  表达查看中版本（选中=品牌色），替代此前的自定义三态彩点（绿/蓝点
  暂弃）与 Button 行；方向键在版本间移动（roving tabindex）；
  轨道 stroke2 2px 保留、对准原生圆点圆心（轨段挂行 wrapper 层——
  Radio 裁剪越界子元素曾致行交界断线）。同日错位修正留痕：圆点为
  Fluent `__indicator`、自带 8px 外边距，圆心在行内 x=16（轨道左
  15px；此前按「根零内边距、圆心 x=8」设 7px 而左偏 8px，且当时的
  「对齐验证」误量了覆盖整行的透明 `__input`）；Radio root 加
  alignItems flex-start——**圆点与标题首行对齐**（2026-09-03 用户指令，
  初版曾垂直居中、多行条目里圆点悬在两行之间即被纠正）；圆点圆心为
  常量距行顶 20px（wrapper padding 4 + indicator 默认 margin 8 + 半径
  8，恰与 label 净偏移下 20px 标题首行中心线重合；vN/时间格以 20px
  行盒对齐同一中心线），轨道锚点随之由 50% 改常量；轨段改为**止于
  圆点上下切点**（上切点 y=12、下切点 y=28；非首行有上段、非末行有
  下段，railDown 止于下行上切点上方 4px 处与下行 railUp 重叠衔接、
  不压圆环），不再贯通穿孔——空心圆点内芯透明，轨线会从内芯露出；行结构
  （2026-09-03 用户指令再调）：修订号 + 两行主列（**第一行修订标题、
  第二行摘要弱化**，取消「·」内联）+ 相对时间列，网格顶对齐、时间
  **统一右缘对齐**（justifySelf end）与标题同线；行间距经行 wrapper
  上下对称 4px padding 放大（对称 padding 不移动 50% 轨道锚点）。
  行宽留痕：RadioGroup 纵向 flex 不拉伸子项、Fluent Radio label
  flex-grow 0，行内容曾整体按内容收缩致时间右对齐失效——wrapper
  加 alignSelf stretch（Griffel 层）+ label flex-grow 1 经全局样式
  `.rev-timeline-radio > .fui-Radio__label`（styles.css，与 .md-body
  同款「Griffel 无后代选择器」刻意例外）两层修复；Radio label 网格
  = 修订号 + 两行主列 + 相对时间；行文字常规字重 400，覆盖 Fluent
  默认 SemiBold，与胶囊/面板标题统一；「当前」文字标注同日用户指令
  移除（选当前版即回传 null）；操作者列同日用户指令随修订模型
  移除 actor、新增修订标题）。
- **关闭逻辑**：点胶囊开合；点击面板外关闭；Esc 关闭；RadioGroup
  形态下**选中不再自动收起**（2026-09-03 用户指令，方向键可连续浏览
  版本，正文实时换装）。
- **版本切换逻辑**（UI-017）：`viewedRevision` 页面本地 state
  （null=当前），不入 URL——刷新回当前版本；点历史版切换、点当前版
  即回当前。版本提示条 MessageBar 已随 2026-09-03 手动对比开关口径
  废除；查看态由时间线选中圆点、胶囊 vN 徽标与 diff 标记自明。

仅 1 条修订时胶囊照常呈现（UI-031 同策略，不隐藏）；自绘组件
（Fluent v9 无现成 Popover 需求匹配的浮动胶囊形态，点外关闭自实现，
不为此引依赖；Fluent Popover 的锚定与滚动行为不合页顶锚定需求，
同此理由自绘）。消费方：条目详情（UI-017）、项目概览（UI-035）。

## 页面容器与标题对齐（2026-09-02，同日七改）

全部页面容器统一口径：padding 上/下 `XL`（20）/ **右 64px** /
**左按整窗 1/4 比例锚定 `max(24px, calc(25vw - 280px))`**（280 =
活动栏收起 48 + 侧栏 232；窗口 < 1216 时钳制下限 24px，最小窗
1024）+ **左对齐，不做水平居中**（无 `margin: 0 auto`）+ 按页面族
限宽（border-box，maxWidth 随 25vw 联动 = `25vw - 280px + 内容宽
上限 + 64px`，内容宽上限不随窗变；设置页无侧栏，左 padding 为
`calc(25vw - 48px)`，最小窗下即 25% 无需钳制，maxWidth 相应少
232）：

| 页面族 | maxWidth | 内容宽上限 |
| --- | --- | --- |
| 条目类型页 / 项目统计 / 项目概览 / 条目详情 | `calc(25vw + 864px)` | 1080 |
| 项目列表 | `calc(25vw + 744px)` | 960 |
| 设置 | `calc(25vw + 656px)` | 640 |
| 任务看板 | 不限宽（`height: 100%` 纵向 flex） | 随区宽 |

沿革：同日先六轮实览调参 24→40→64→320→260→200 定稿固定左 200；
随即七改（用户指令「按整个页面的 1/4 处开始」）——固定像素只在
调参窗口成立：200 = 1920 整窗 1/4（480）减左栏 280，换窗比例即漂
（1280 下标题落到整窗 37.5%），即「页边距始终不对」的根源。比例
锚定后标题起点恒为整窗 1/4 分界，任意窗口视觉一致；1920 宽下与
六改终版逐像素等值（padding 200 / maxWidth 1344）。文章阅读列
（概览/详情）内容宽上限自 860 上调 1080 维持不变（用户指令正文列
太窄）。废止更早的 `margin: 0 auto` 居中版式：各页限宽不同，居中
使标题起点随窗口宽度与页面族漂移（用户指令：所有页面标题对齐到
同一起点；任务看板页本就未居中，即对齐基准）。

已知偏差（留痕）：① 活动栏展开（48→200）时标题随内容区右移
152px，比例锚定以默认收起态为基准；② `25vw` 含滚动条宽（Windows
经典滚动条约 17px），标题实际起点右偏约 4px，实览不可辨。

实现收敛：口径落 `src/components/usePageContainerStyles.ts`
（七页共用，页面族 variant 入参），页面不再各自手写
padding/maxWidth。AppShell main 自身无 padding，页面容器是唯一
边距层。

## 头部元信息 chip（2026-09-03，2026-09-04 二改）

页头元信息统一 chip 形态：1px `colorNeutralStroke2` 边框 +
`borderRadiusSmall` 圆角 + 上下 2px 左右 `S`(8) 内边距 + 字号
`fontSizeBase200`（12px），颜色继承头部元信息行弱化灰
（Foreground3）；规格落共享 `MetaChip` 组件（src/components/，
原条目详情页本地 metaChip 样式上收）。

**二改（2026-09-04 用户指令）：修订元信息拆为两枚 chip**——首枚
**版本身份 chip**：查看当前版显示「当前版本 vN」、查看历史版显示
「旧修订版本 vN」（文案随查看态实时切换；版本身份前缀同日用户
指令由 r 改 v，修订号语义不变）；次枚**相对时间 chip**：
随查看版显示对应修订时间（查看历史版时为该版快照的修订时间，
不再是恒显当前版）。适用：条目详情页（UI-016）与项目概览页
（UI-035）；metadata 键值对 chip 与替代者跳转链接（保持链接形态
不 chip）不变。

## 页面内容渐入（2026-09-04，同日五改）

路由进入时页面内容做一次入场浮现：透明度 0→1 + 纵向
`translateY(16px)→0`（同日二改：用户指令由对齐侧栏的 8px 上调
16px；侧栏条目入场保持 8px 不同步），时长 **400ms**（四改：用户
反馈「速度太快」由 `durationNormal` 200ms 上调，令牌外值留痕）、
缓动 `curveDecelerateMid`，`animation-fill-mode: backwards`；「减弱动态」
以 no-preference 反向门控（与「侧栏条目入场动画」同一套全局
styles.css keyframes + 类的手法——Griffel makeStyles @media 槽位
不生成规则留痕）。类名 `page-enter` 落 `usePageContainerStyles`
统一注入，七页容器一处生效。

**三改（同日用户指令「调整为方案三看看」）：条目详情/项目概览的
内容层拆两块错落**——头部层（返回链接/标题/元信息 chip）delay 0
先入，正文层随后：PageFadeIn 增 `delay` 入参，经行内
`--page-enter-delay` 变量注入，`.page-enter` 增
`animation-delay: var(--page-enter-delay, 0ms)`（backwards fill 保证
延迟期保持首帧不可见）。**步进 140ms**（四改同调，原 80ms）、总量
钳制 420ms（同侧栏哲学），未来更多分块照此接续；其余五页维持
容器整体渐入。原「预留升级」条款随本改落地。

**五改（同日用户指令「每层依次上浮就像 sidebar 出现一样」）：
项目统计页内容拆四块错落**——标题层 0、状态卡行 140、图表行
280、最近修订卡 420（步进 140 不变，末层恰落总量钳制 420ms）。
该页无 tree 内 fixed 元素，改走内容层的动因是分层而非胶囊（留痕）：
容器级整块注入关闭（`usePageContainerStyles("workbench", false)`），
四块经 PageFadeIn 依次入场；骨架屏/错误态/空态三分支不播入场
（骨架屏本身即加载示能）。使用内容层的页面由两页变三页，容器
整体渐入余四页。

**胶囊页例外（留痕）**：条目详情/项目概览的浮动胶囊面板是 tree 内
`position: fixed` 元素，transform 会使动画祖先临时成为 fixed 的
包含块——若整容器参与动画，胶囊在 200ms 动画期内改锚页面容器而
跳位、动画结束弹回视口角。故两页改以共享 `PageFadeIn`（同款类名
的内容层 div）包裹胶囊**之外**的头部与正文，胶囊不入动画。

重播语义：路由变更引发页面重挂才重播（进/出项目、概览⇄详情、
列表⇄详情）；同路由参数级导航（条目类型页之间）组件复用不重挂
即不重播（与侧栏「层内导航不重播」同哲学）；页内交互（版本切换、
diff 开关）不重播。

## 侧栏条目入场动画（2026-09-02）

第二层导航栏（UI-001，进入式两级呈现）的条目行、组头行、题行在
**层级进入**时做一次入场浮现：透明度 0→1 + 纵向 `translateY(8px)→0`，
时长 `durationNormal`（200ms）、缓动 `curveDecelerateMid`，按 DOM 序
逐项错开 `16ms`、错开总量钳制 240ms（`animation-fill-mode: backwards`
保证延迟期保持首帧不可见）。动画类整体落 styles.css 全局
`.sidebar-enter`（.md-body 同款刻意例外）：Griffel 不透出 keyframes
工具（不为其引 @griffel/react 直依赖），且**实测其 makeStyles 的
@media 槽位不生成规则**（全页 83 条 media 规则均出自 makeResetStyles，
留痕），动画声明与降级门控须一并落全局；错开经行内 CSS 变量
`--enter-delay` 注入单一类，不逐项造类。

触发语义（2026-09-02 用户指令「这些 item 做入场动画」，范围限第二层
侧栏）：**只在层级切换时播一次**——进入层级 ⇄ 根层级、设置页往返
（侧栏整体卸载重挂）时新条目集挂载即播；**层内导航不重播**（概览→
统计→类型页，DOM 节点复用无重挂）；数据 refetch 重渲染亦不重播。
两层级同规格（根层级项目清单行、进入层级返回行/题行/子导航/组头）。

「减弱动态」降级：动画类由 `@media (prefers-reduced-motion:
no-preference)` **反向门控**——仅在未偏好减弱动态时声明动画，开启
偏好即不匹配规则、直接呈现终态。不在 Griffel 类内写 @media 覆盖
（实测不生成规则，见上；亦不触碰「盒模型」节记载的 Griffel @media
桶序坑——该坑仅在 @media 与 :hover 同抢 transform 时反向）。

与共享指示条的互作（留痕）：条目 transform 不影响 `offsetTop/offsetLeft`
布局值，指示条定位（indicatorMotion 测量的即布局位）不受入场动画
干扰；指示条元素本身常驻不重挂，不参与入场。

实现收敛：`src/styles.css`（`@keyframes sidebar-item-enter` +
`.sidebar-enter` 类，no-preference 门控，令牌走主题 CSS 变量）+
`src/app/layout/Sidebar.tsx`（mergeClasses 挂全局类 + `--enter-delay`
错开变量），规格与触发语义以本节为准。
