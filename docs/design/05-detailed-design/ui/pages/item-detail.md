# 条目详情页

> 状态：待确认
> 路由：`/projects/:id/items/:code`
> 关联：UI-015～018、FR-009/010/013、BR-004、UC-011/012/015、CMP-001

## 布局（UI-015/016）

独立页，单栏滚动（容器内容宽 1080 左对齐，见 patterns.md
「页面容器与标题对齐」，2026-09-02 修订——原文「全宽」与实现不符），
区块顺序（2026-09-03 用户指令：关联区与影响定位区自正文迁入浮动
胶囊面板，正文收窄为 头部 + 正文 两区块）：

1. **头部**：面包屑（项目 / 类型组 / 返回列表）+ 编号（等宽大字）+
   标题 + 状态徽章 + 元数据摘要（结构化 metadata 键值对：优先级、
   备注等）+ 当前修订号与更新时间；已替代条目在头部显著标注替代者
   编号（superseded_by，可跳转）；
2. **正文**：Markdown 渲染（react-markdown，只读，无分栏编辑形态——
   CON-009）；
3. **详情面板浮动胶囊**（2026-09-03 用户指令由「修订历史」胶囊扩为
   三分区面板，壳层见 patterns.md「浮动胶囊面板」）：单胶囊 fixed
   悬浮视口右上（同日定位修订：距右缘 24px、顶距 24px；「详情面板」+
   计数徽标：查看历史版时显 rN，否则显
   修订数；查看历史版时胶囊呈选中底色），面板无名称标题行（同日二改：
   收起按钮改居首分区小节标题行右侧）；三分区以全宽 hairline 分隔、
   小节标题与内容最左元素统一左缘、内容超高内部滚动；小节标题区可
   点击折叠/展开对应分区（2026-09-03 用户指令：标题后 chevron 旋转
   指示 + 高度过渡；折叠分区 ≠ 收起面板，状态本地不持久化）：
   - **修订历史**（FR-009/UI-017）：修订号 + 摘要 + 时间 + 标题
     （RadioGroup 时间线形态，见 patterns.md）；点选历史版本时正文区
     自动显示与紧邻上一版的**单栏 diff**（2026-09-03 用户指令，纯前端
     MDAST 语义比较，见 patterns.md「修订对比」；最旧版本无上一版 →
     显示该版完整快照 content_snapshot，只读），头部出现「正在查看
     rN（与 rM 对比）」提示条与「回到当前」动作；
   - **关联**（FR-010）：按关系类型分组（derives/depends/satisfies/
     traces/relates），每组分上下游（该条目指向的 / 指向该条目的），
     每项 = 编号 + 标题 + 方向，点击跳转目标详情；数据恒为当前状态
     （关系不随修订快照变化）；
   - **影响定位**（UI-018）：受影响条目与任务按类型分组清单，逐项
     可跳转；遍历语义（derives/satisfies/depends 入边反向闭包）由
     `get_impact` 承载，UI 只呈现；数据恒为当前状态。

```text
← 返回 · relay-harbor / FR 功能需求        (📋详情面板·3)
FR-001  用户登录              [已确认]
优先级 P1 · r3 · 2 分钟前
────────────────────────────
## 需求描述（Markdown 渲染）

面板展开（同位变形，右对齐；无名称标题行，收起钮在首分区标题行右）：
┌───────────────────────────┐
│ 修订历史 ⌄           [收起] │
│ ◉ r3 同步基线 …  ○ r2 ○ r1 │
│ ───────────────────────── │
│ 关联 ⌄                      │
│ 指出 FR-014 Markdown 导出  │
│ ───────────────────────── │
│ 影响定位 ⌄                  │
│ 1跳 TASK-007 · DEC-002 …  │
└───────────────────────────┘
```

## 数据

- `get_item_detail` → `["projects", id, "item", code]`；
- `get_item_revisions` → `["projects", id, "item", code, "revisions"]`
  （历史版本内容经 include revisions 或详情载荷，见 api-contracts）；
- `get_relations` → `["projects", id, "item", code, "relations"]`；
- `get_impact` → `["projects", id, "impact", code]`。

## 交互

- 全部只读；跳转均为路由导航（可后退）；
- 版本切换不改变 URL（查看态），刷新回到当前版本。

## 状态与边界

- 编号不存在：错误态「条目不存在」（可能已被项目删除）；
- 无关联/无影响：面板分区内显示「无」而非隐藏（结构稳定，2026-09-03
  随区块迁移生效）；
- 仅 1 条修订：胶囊照常呈现 r1 当前（UI-031 同策略）。

## 测试要点

历史版本快照与 content_snapshot 一致（BR-004 只读）；关联上下游方向
正确；影响清单与 get_impact 一致；superseded 条目的替代者跳转。
